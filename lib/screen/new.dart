import 'package:flutter/material.dart';

class Exoscreen extends StatelessWidget {
  const Exoscreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

       appBar: AppBar(
         title: Text("helo"),
       ),
       
      
    );
  }
}